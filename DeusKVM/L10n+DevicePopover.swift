import SwiftUI

extension L10n {
    enum DevicePopover {
        static var rename: LocalizedStringKey {
            "device_popover.rename"
        }

        static var save: LocalizedStringKey {
            "device_popover.save"
        }

        static var cancel: LocalizedStringKey {
            "device_popover.cancel"
        }
    }
}
